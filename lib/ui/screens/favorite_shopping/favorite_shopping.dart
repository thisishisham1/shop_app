import 'package:flutter/material.dart';

class FavoriteShopping extends StatelessWidget {
  const FavoriteShopping({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Container(
          child: Text("favorite"),
        ));
  }
}
